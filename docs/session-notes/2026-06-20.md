# Session Notes — 2026-06-20

## Completed & merged to `main` (5 squash-merged PRs, all CI-green)

### Foundation (earlier in session)

- **#1 scaffold** — verified booting on web + iPhone + iPad sims; Metro/Jest fixed for the pnpm layout; committed.
- **7 subagents** created in `.claude/agents/` (db-architect, edge-fn-eng, design-translator, mobile-screen-builder, web-screen-builder, test-author, code-reviewer; scaffolder skipped — already spent).
- **#2** ESLint/Prettier/Husky — verified already shipped by the scaffold.
- **#3 CI** — `pr-checks.yml` (lint+typecheck / test / build-web / mobile-typecheck); later fixed to **Node 22** and given placeholder web-build env.
- **#4 Supabase local** — `supabase init` + stack up; DB/REST/Auth reachable.
- **#5 schema** — 15 tables (incl. `savings_goals`) + 8 enums + indexes; types generated.
- **#7 client + env** — shared `@lootloop/client` wrapper + connection probe; web + mobile (react-native-config) wired.
- **GitHub remote** established (`samipdesai/lootloop`); CI proven green on real Actions.

### Headline work (PRs #1–#3)

- **#6 — RLS + atomic functions** (PR #1): Row-Level Security on all 14 family tables; parent + kid (JWT-claim) family resolution; `award_points_on_approval`, `purchase_reward`, `transfer_to_savings`, `credit_interest` with `FOR UPDATE` locks, idempotency, and **in-function caller authorization**. 23 assertions incl. adversarial.
- **#9 — kid-auth Edge Function** (PR #2): PIN → bcrypt verify → HS256 JWT matching the 002 claim contract; byte-identical no-leak 401s; parameterized query.
- **#8 — parent auth: backend + full web** (PR #3): `004` bootstrap (`create_family_and_parent`, `create_family_invite`, `join_family_as_parent`) + `family_invites` RLS + 15 tests; shared auth helpers + `mapAuthError`; **6 Next.js screens** (login, signup, check-email, forgot/reset password, create-or-join onboarding) with `@supabase/ssr` cookie sessions + route gating.

## Key decisions locked

- **Kid JWT claim contract** (`ll_role` / `family_id` / `profile_id`, 30-day TTL) — binds future kid-auth work; documented in `002`'s header.
- **Atomic SQL functions must self-authorize the caller** (saved to memory `atomic-fn-caller-authz`) — the client calls RPC directly; there is no trusted server.
- **#8 scope:** email confirmation **ON**, password reset **in**, co-parent **join via invite code**, **Sign in with Apple deferred**, password min 8.
- CocoaPods retained (SPM → Phase 2); pnpm 11.8 requires Node 22.

## Issues caught at the review gate

- **Atomic-function authZ hole** in #6 (a kid could have self-approved chores / spent another kid's points / minted interest) — fixed before commit.
- **Kid-session guard** added to the #8 bootstrap functions (a kid JWT slipped past the "no existing profile" check; only the FK stopped it).
- **CI build-web failure** (missing `NEXT_PUBLIC_*` at `next build`) — fixed with placeholder public env; verified the build never connects.
- **OrbStack daemon wedge** — recovered remotely via `orbctl stop/start`; all shell probes now use fork-based hard timeouts (macOS lacks `timeout`).

## What remains

### Milestone 1 finish (UI)

- **#8 mobile** parent-auth screens — backend + web done; reuse the same `@lootloop/client` helpers + `design/specs/08-parent-auth.md`. Needs an async-storage adapter for supabase-js session persistence (web used `@supabase/ssr` cookies; mobile differs) and an interim `useSizeClass` shim until #10.
- **#10** adaptive nav shells (web layout + mobile RootNavigator: ParentTabs/SplitView, KidTabs/SplitView by role + size class).
- **#9 kid PIN login screen** (mobile, depends on #10) — also needs the "how does the device learn `family_id` pre-auth" decision (cached session / family code / deep link).

### Deferred / ops (tracked in task list)

- Enable **branch protection** on `main` (recommended next — a proven green gate now exists).
- Resolve **Node-version policy** (plan says Node 20; pnpm 11.8 needs 22; CI on 22, local on 20).
- Clean up the **`pnpm` package.json field** warning (`onlyBuiltDependencies` moved per pnpm 11).
- **Supabase cloud project + deploy/release CI** (Vercel web preview + Fastlane iOS; needs secrets/accounts).
- **Sign in with Apple / OAuth** (blocked on Apple Developer account).
- **CocoaPods → SPM** migration (Phase 2).
- **Reading-approval atomic award fn** (#28) — follow the `atomic-fn-caller-authz` convention.
- **#7 on-sim** runtime connection-log check (iPhone + iPad).
- Served-function + auth **E2E in CI** (kid-auth + parent-auth flows).

## State for next session

- `main` is clean and up to date; local OrbStack/Supabase stack is running (recovered).
- All deferred items are in the Claude task list.
- Suggested first action next session: **enable branch protection**, then start **#8 mobile** (or **#10 nav shells** to unblock the kid screen).
