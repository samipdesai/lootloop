import { PlaceholderPage } from '@/components/dashboard/PlaceholderPage';

export default function RewardsPage() {
  return (
    <PlaceholderPage
      title="Rewards"
      emoji="🎁"
      blurb="Build your reward store — create items kids can buy with the points they earn."
      taskRef="Coming in task #22"
    />
  );
}
