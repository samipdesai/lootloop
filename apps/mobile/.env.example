# Copy to apps/mobile/.env and fill from `supabase status` (local) or your Supabase project (cloud).
# Loaded natively at build time via react-native-config. Never put the service-role key here.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
