# Supabase — get values from `supabase start` output
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # server-only, never bundle into client builds

# App
APP_ENV=development
